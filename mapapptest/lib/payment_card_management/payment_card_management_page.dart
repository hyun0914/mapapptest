import 'package:flutter/material.dart';

import 'material/body.dart';

class PaymentCardManagementPage extends StatelessWidget {
  const PaymentCardManagementPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
